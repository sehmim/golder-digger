# Golders Desktop

Electron, React, TypeScript, and Vite boilerplate for the Gold Digger desktop interface.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Create an unpacked application with `npm run package`, or a macOS installer with `npm run dist`.
